spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/labreport?serverTimezone=UTC
    username: root
    password: 105036
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

logging:
  #具体使用时提高写入日志的级别，warn级别写入日志文件
  level:
    root: warn
    com.watermleon: inofr
  file: log\blog-product.log

server:
  port: 8080
